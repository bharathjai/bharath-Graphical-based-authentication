import React from "react";
import Parallax from "./Parallax"; // Import Parallax component

function Home() {
  return (
    <div>
      <Parallax /> {/* Add the Parallax component to the page */}
      {/* Other components or content for the Home page */}
    </div>
  );
}

export default Home;
